CREATE TABLE `app_clrestapi`.`client_credentials` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `base_url` VARCHAR(45) NULL,
  `username` VARCHAR(45) NULL,
  `api_key` VARCHAR(45) NULL,
  `api_path` VARCHAR(45) NULL,
  `password` CHAR NULL,
  PRIMARY KEY (`id`));
